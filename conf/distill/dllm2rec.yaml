ranking_loss_weight: 1.0
embedding_loss_weight: 1.0
ce_loss_weight: 1.0
ranking_temperature: 2.0
embedding_loss_type: mse # mse or cosine
teacher_checkpoint_path: /content/drive/MyDrive/rec/anyo/result/teacher_20251120_082122/checkpoints/teacher-epoch=00-val_loss=8.3591.ckpt
teacher_outputs_batches_dir: /content/drive/MyDrive/rec/anyo/result/teacher_20251120_082122/teacher_outputs_batches
# student_checkpoint_path: null # 学習済み生徒モデルのパス (蒸留の初期化に使う場合)
ed_weight: 0.1
gamma_position: 1.0
gamma_confidence: 1.0
gamma_consistency: 1.0
candidate_topk: 10
alpha: 0.0 # Weight for DRO loss. Set to > 0 to enable.
beta: 1.0  # Parameter for DRO loss (robust radius).
ps_power: 0.05 # Power for propensity score calculation.
lam: 0.8 # Weight for importance-aware ranking distillation
num_neg_samples: 1 # Number of negative samples for ranking distillation
