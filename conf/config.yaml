defaults:
  - _self_
  - dataset: movielens
  - train: default
  - teacher: ilora
  - student: sasrec
  - distill: dllm2rec
  - eval: default

# Basic experiment settings
seed: 42

run:
  dir: "result/result_${now:%Y%m%d_%H%M%S}"
job:
  chdir: true