defaults:
  - _self_
  - experiment: bigrec_movielens
  - post_training: default

# Basic experiment settings
seed: 42

hydra:
  run:
    dir: "result/post_training_${now:%Y%m%d_%H%M%S}"
  job:
    chdir: true
